# ProtoShield security scan outputs (local artifacts, not source)
.protoshield/scans/

# Docusaurus build-time generated output (regenerated by build; must not be
# hand-edited). Excluded from source control and from security scans since
# findings on generated files are false positives.
website/.docusaurus/
website/build/
